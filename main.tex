\documentclass[a4paper,11pt]{article}

\usepackage[english]{babel}
% \usepackage[portuguese]{babel}
%\usepackage[T1]{fontenc}
%\usepackage[latin1]{inputenc}
\usepackage{amsmath, amssymb}
\usepackage{graphicx}
\usepackage{setspace}
\usepackage{float}

\usepackage{color}
\usepackage{listings}

\oddsidemargin 0.22in
\textwidth 5.8in

\usepackage[skins]{tcolorbox}
\definecolor{AB}{rgb}{0,0.3961,0.7412}
\usepackage[bookmarksnumbered=true]{hyperref}
\hypersetup{
     colorlinks = true,
     linkcolor = AB,
     anchorcolor = AB,
     citecolor = AB,
     filecolor = AB,
     urlcolor = AB
}

\renewcommand{\lstlistingname}{Listado}
\lstset{
  language=Python,
  basicstyle=\ttfamily\footnotesize,
  backgroundcolor=\color{gray!10},
  keywordstyle=\color{blue},
  commentstyle=\color{green!60!black},
  stringstyle=\color{orange},
  frame=single,
  breaklines=true
}

\renewcommand\labelitemi{$\leadsto$}
\usepackage{footmisc}

\usepackage{enumitem}
\setlist[itemize]{leftmargin=*}
\setlist[enumerate]{leftmargin=*}
\renewcommand{\labelitemi}{$\circ$}

\usepackage{scrextend}
\deffootnote[1em]{1em}{1em}{\textsuperscript{\thefootnotemark}\,}

\usepackage{titlesec}
\titleformat{\section}{\fontsize{13}{14}\sc}{\thesection}{1em}{}[]
\titleformat{\subsection}{\fontsize{11}{12}\sc}{\thesubsection}{1em}{}[]

 \setlength{\parindent}{0pt}

\usepackage{tocloft}
\renewcommand{\cfttoctitlefont}{\sc}
\renewcommand{\cftsecfont}{\sc}
\renewcommand{\cftsubsecfont}{\sc}

\begin{document}

%%%%%%%%%% Cover page %%%%%%%%%%%
{\Large \noindent \sc Project Work - Sistema Mecânico} \\ % Alterado para um título genérico

\begin{spacing}{1.5}
{\large\sc \noindent Nome completo: Paulo Arthur Farias Queiroz}
% {\large\sc \noindent Nome completo:}\\

{\large\sc \noindent Numero de matricula: 552919}
% {\large\sc \noindent Nome completo:}\\
\end{spacing}

\begin{spacing}{1.5}
{\large\sc \noindent Nome completo: Filipe Abner Alves Caetano}
% {\large\sc \noindent Nome completo:}\\

{\large\sc \noindent Numero de matricula: 553960}
% {\large\sc \noindent Nome completo:}\\
\end{spacing}

\url{https://github.com/FilipeCaetano04/Sistema-Controle-PW.git}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section*{\sc Tarefa 1: Modelagem}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection*{\sc Questão 1.1}

\subsubsection*{\sc Solução}
O sistema mecânico descrito nos enunciados é uma representação simplificada de um veículo, frequentemente referido como modelo cinemático de bicicleta devido às suas simplificações geométricas. Um modelo cinemático se concentra nas relações geométricas do movimento, ou seja, como a posição, velocidade e orientação mudam ao longo do tempo, sem considerar as forças ou torques que causam esse movimento.
\\\\
Nesta resolução, objetivo é obter um modelo de espaço de estados para o sistema mecânico de direção do veículo. Um modelo de espaço de estados é uma representação matemática de um sistema físico como um conjunto de equações diferenciais de primeira ordem.
\\\\
Sistemas dinâmicos como o da questão podem ser descritos por relações entrada-saida ou por variáveis internas denominadas variáveis de estado. O espaço de estados irá nos fornecer um modelo matemático de como o veículo se move.
\\\\
A seguir, são especificados os elementos para obtenção do modelo.
\\\\
Variáveis de estado: 
\[
    \textbf{x(t)} = 
    [x_{1}(t), x_{2}(t), x_{3}(t)]
    =
    [x_{1}(t), x_{2}(t), \theta(t)]
\]
\begin{itemize}
    \item $x_1(t)$: posição longitudinal (m)
    \item $x_2(t)$: posição lateral (m)
    \item $\theta(t) = x_3(t)$: orientação (rad)
\end{itemize}

Vetor de entradas:
\[
    \textbf{u(t)} = 
    [u_{1}(t), u_{2}(t)]
    =
    [v(t), \delta(t)]
\]
\begin{itemize}
    \item $u_1(t) = v(t)$: velocidade do centro de massa (m/s)
    \item $u_2(t) = \delta(y)$: ângulo de direção (rad)
\end{itemize}

Vetor de saídas:
\[
    \textbf{y(t)} = [x_{1}(t), x_{2}(t)]
\]

O modelo de espaço de estados não linear é geralmente representado pela forma :
\[
    \dot{\textbf{x}} = f(\textbf{x(t)}, \textbf{u(t)})
    =
    \begin{bmatrix}
        f_1(\textbf{x(t)}, \textbf{u(t)}) \\
        f_2(\textbf{x(t)}, \textbf{u(t)}) \\
       f_3(\textbf{x(t)}, \textbf{u(t)})
    \end{bmatrix}
    =
    \begin{bmatrix}
        \dot{x_1} \\
        \dot{x_2} \\
        \dot{x_3}
    \end{bmatrix}
\]
\[
    \textbf{y} = g(\textbf{x(t)}, \textbf{{u(t)}}) = 
    \begin{bmatrix}
        g_1(\textbf{x(t)}, \textbf{u(t)}) \\
        g_2(\textbf{x(t)}, \textbf{u(t)})
    \end{bmatrix} =
    \begin{bmatrix}
        x_1 \\
        x_2
    \end{bmatrix}
\]

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{imagens/veiculo_4rodas.png}
    \caption{Veículo com quatro rodas.}
    \label{fig:veiculo_com_4rodas}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{imagens/veiculo_simplificado.png}
    \caption{Veículo simplificado.}
    \label{fig:meu_diagrama_simplificado}
\end{figure}

A obtenção do modelo de espaço de estados para este sistema envolve a decomposição da velocidade do centro de massa em componentes longitudinal e lateral, e o cálculo da taxa de orientação ($\dot{\theta}$) utilizando o Centro Instantâneo de Rotação (CIR). As equações resultantes são não lineares e descrevem como as variáveis de estado mudam com base nas entradas.

\[
    \dot{\textbf{x}} =
    \begin{bmatrix}
        \dot{x_1} \\
        \dot{x_2} \\
        \dot{\theta}
    \end{bmatrix} 
    =
    \begin{bmatrix}
        v_{x1} \\
        v_{x2} \\
        \omega
    \end{bmatrix}
    =
    \begin{bmatrix}
        v \cdot \cos{(\alpha + \theta)} \\
        v \cdot \sin{(\alpha + \theta)} \\
        \dfrac{v}{R}
    \end{bmatrix}
\]

\begin{itemize}
    \item $v$: magnitude da velocidade do veículo 
    \item $v_{x1}$: componente horizontal da velocidade
    \item $v_{x2}$: componente vertical da velocidade
    \item $\omega$: velocidade angular de rotação
    \item $R$: raio da rotação
    \item $\alpha$: ângulo de deslizamento
    \item $\alpha + \theta$: ângulo de direção da velocidade 
\end{itemize}

Faremos algumas manipulações trigonométricas nas expressões atuais de $\dot{\textbf{x}}$ com o objetivo de deixá-las em função dos parâmetros das variáveis de estado e de entrada do sistema. Observando a Figura~\ref{fig:meu_diagrama_simplificado}, temos que:
\[
    \tan(\delta) = \frac{b}{L'}
\]
\[
    L' = \frac{b}{\tan(\delta)}
\]
Onde $b$ é o comprimenro do veículo. Além disso, observamos:

\[
    \tan(\alpha) = \frac{a}{L'}
\] 
\[
    \tan(\alpha) = \frac{a}{\frac{b}{\tan(\delta)}} =\frac{a \cdot \tan(\delta)}{b}
\]
\[
    \alpha = \arctan(\frac{a \cdot \tan(\delta)}{b})
\]
Onde $a$ é o comprimento do centro de massa até a roda inferior. Agora vamos analisar o raio da rotação:

\[
    \sin{(\alpha)} = \frac{a}{R}
\]
\[
    R = \frac{a}{\sin{(\alpha)}}
\]
\[
    R = \frac{a}{\sin{(\arctan(\frac{a \cdot \tan(\delta)}{b}))}}
\]

Agora, conhecemos todas as expressões do espaço de estados encontrado mais acima com os parâmetros que desejamos. Dessa forma, temos:

\[
    \dot{x_1} = v \cdot \cos{(\alpha + \theta)}
\]
\[
    \dot{x_2} = v \cdot \sin{(\alpha + \theta)}
\]
\[
    \dot{\theta} = \dfrac{v}{R}
\]
Substituindo $\alpha$ e R:

\[  
    \dot{x_1} = v \cdot \cos{(\arctan(\frac{a \cdot \tan(\delta)}{b}) + \theta)}
\]
\[
    \dot{x_2} = v \cdot \sin{(\arctan(\frac{a \cdot \tan(\delta)}{b}) + \theta)}
\]
\[
    \dot{\theta} = \frac{v \cdot \sin{(\arctan(\frac{a \cdot \tan(\delta)}{b}))}}{a}
\]

Reescrevendo as equações utilizando o vetor de estado e o vetor de entrada:

\[  
    \dot{x_1} = u_1 \cdot \cos{(\arctan(\frac{a \cdot \tan(u_2)}{b}) + x_3)}
\]
\[
    \dot{x_2} = u_1 \cdot \sin{(\arctan(\frac{a \cdot \tan(u_2)}{b}) + x_3)}
\]
\[
    \dot{x_3} = \frac{u_1 \cdot \sin{(\arctan(\frac{a \cdot \tan(u_2)}{b}))}}{a}
\]

\subsection*{\sc Questão 1.2}

\subsubsection*{\sc Solução}

Para esta resolução, iremos realizar a análise do ponto de equilíbrio do sistema. Como a questão pede, vamos dividir a análise em duas etapas: A verificação de equilíbrio para uma entrada constante e a determinação do ponto de equilíbrio para a condição de movimento retilíneo.
\\\\
Antes de iniciar os cálculos, é fundamental definir o que é um ponto de equilíbrio. Um sistema dinâmico, descrito por um vetor de estados $\vec{x}$, está em um ponto de equilíbrio quando a taxa de variação de todos os seus estados é nula. Matematicamente, isso é expresso como:
\begin{equation}
    \dot{\vec{x}} = \vec{0}
\end{equation}
Para o nosso sistema, isso significa que as três condições a seguir devem ser satisfeitas simultaneamente:
$$ \dot{x_1} = 0, \quad \dot{x_2} = 0, \quad \dot{x_3} = 0 $$
\\\\\\ 
Para a resolução, nos são dados os seguintes valores: $\bar{u} = [v, \delta] = [10 \text{ m/s}, 0.1 \text{ rad}]$.
\\\\
A estratégia consiste em verificar se todas as derivadas de estado são nulas, caso uma delas não seja, podemos concluir que para a entrada dada não há equilíbrio.
\\\\
A equação para $\dot{x_3}$ obtida na resolução da Questão 1.1 é:
\begin{equation} \label{eq:x3_dot}
    \dot{x_3} = \frac{u_1 \cdot \sin\left(\arctan\left(\frac{a \cdot \tan(u_2)}{b}\right)\right)}{a}
\end{equation}
Substituindo os valores fornecidos ($u_1 = 10$, $u_2 = 0.1$, $a = 1.5$, $b = 3.0$):
\begin{equation}
    \dot{x_3} = \frac{10 \cdot \sin\left(\arctan\left(\frac{1.5 \cdot \tan(0.1)}{3.0}\right)\right)}{1.5}
\end{equation}
Analisando a expressão no numerador:
\begin{itemize}
    \item O termo $\tan(0.1)$ é um número positivo.
    \item O argumento da função $\arctan$ é, portanto, um número positivo.
    \item O $\arctan$ de um número positivo é um ângulo positivo.
    \item O $\sin$ de um ângulo pequeno e positivo também é um número positivo.
\end{itemize}
Dessa forma, o numerador é um número diferente de zero. Como o denominador também não é zero, a expressão final para $\dot{x_3}$ é diferente de zero.
\\\\
Portanto, como $\dot{x_3} \neq 0$, a orientação do veículo está em constante mudança. Isso viola a condição de equilíbrio. Dessa forma, não é possível encontrar um estado de equilíbrio para o sistema com a entrada constante fornecida.
\\\\
A segunda etapa da análise nos pede para encontrar o ponto de equilíbrio para os estados $(x_2, x_3)$ e a entrada $u_2$ que satisfazem a condição de movimento em linha reta com $\theta = 0$.
\\\\
Aqui, o processo é inverso. Partimos da condição de equilíbrio desejada para encontrar a entrada que a causa. A condição para movimento retilíneo é que a orientação seja constante, o que implica em uma velocidade angular nula, $\dot{x_3} = 0$.
\\\\
Utilizando a Equação \ref{eq:x3_dot} e igualando-a a zero:
$$ \frac{u_1 \cdot \sin\left(\arctan\left(\frac{a \cdot \tan(u_2)}{b}\right)\right)}{a} = 0 $$
Como $u_1 \neq 0$ e $a \neq 0$, a fração só pode ser nula se a parcela for nula:
$$ \sin\left(\arctan\left(\frac{a \cdot \tan(u_2)}{b}\right)\right) = 0 $$
A função seno é zero somente quando seu argumento é zero:
$$ \arctan\left(\frac{a \cdot \tan(u_2)}{b}\right) = 0 $$
A função arcotangente é zero somente quando seu argumento é zero:
$$ \frac{a \cdot \tan(u_2)}{b} = 0 $$
Como $a$ e $b$ não são nulos, a única possibilidade é:
$$ \tan(u_2) = 0 \implies \bar{u}_2 = 0 $$
Portanto, a entrada de equilíbrio para o volante é $\bar{u}_2 = 0$ radianos.
\\\\
Com a entrada de equilíbrio em mãos, determinamos os estados correspondentes.
\begin{itemize}
    \item Para $x_3$ (Orientação): O enunciado define o cenário de interesse como um caminho reto onde $\theta=0$. Logo, o estado de equilíbrio para a orientação é $\bar{x}_3 = 0$.
    \item Para $x_2$ (Posição Lateral): Devemos verificar a consistência, garantindo que $\dot{x_2}=0$ nessas condições. A equação para $\dot{x_2}$ é:
    \begin{equation}
        \dot{x_2} = u_1 \cdot \sin\left(\arctan\left(\frac{a \cdot \tan(u_2)}{b}\right) + x_3\right)
    \end{equation}
    Substituindo os valores de equilíbrio $\bar{u}_2 = 0$, $\bar{x}_3 = 0$ e $u_1 = 10$:
    $$ \dot{x_2} = 10 \cdot \sin\left(\arctan\left(\frac{1.5 \cdot \tan(0)}{3.0}\right) + 0\right) $$
    $$ \dot{x_2} = 10 \cdot \sin\left(\arctan(0) + 0\right) = 10 \cdot \sin(0) = 0 $$
    Como $\dot{x_2}=0$, a velocidade lateral é nula, confirmando o equilíbrio. Definimos a trajetória retilínea sobre o eixo x do sistema de coordenadas, resultando no estado de equilíbrio para a posição lateral $\bar{x_2} = 0$.
\end{itemize}


Concluindo, a análise nos permite definir que o ponto de equilíbrio para os estados de interesse $(x_2, x_3)$, sob a condição de movimento retilíneo com $\theta=0$, é:
$$ (\bar{x}_2, \bar{x}_3) = (0, 0) $$
Este equilíbrio é alcançado com uma entrada de controle no volante de:
$$ \bar{u}_2 = 0 \text{ rad} $$

\subsection*{\sc Questão 1.3}

\subsubsection*{\sc Solução}

Nesta seção, determinaremos as matrizes (A, B, C, D) do sistema linearizado em torno do ponto de equilíbrio encontrado na questão anterior.

O modelo não linear da Questão 1.1 é preciso, mas matematicamente complexo. A linearização é uma técnica para criar um modelo linear muito mais simples, que aproxima o comportamento do sistema real em uma pequena região em torno de um ponto de operação específico (o ponto de equilíbrio). Uma analogia seria um zoom aplicado em uma estrada curva em um mapa. Este modelo linear é representado pela forma padrão de espaço de estados:
$$ \dot{\delta x} = A \cdot \delta x + B \cdot \delta u $$
$$ \delta y = C \cdot \delta x + D \cdot \delta u $$
As matrizes A e B são obtidas pelo cálculo das derivadas parciais (o Jacobiano) das funções de estado, avaliadas no ponto de equilíbrio. Cada elemento dessas matrizes representa a "influência" ou "sensibilidade" de uma variável sobre a outra.
\\\\
Os cálculos serão realizados em torno do ponto de equilíbrio:
$$ (\bar{x}_2, \bar{x}_3) = (0, 0), \quad \bar{u}_2 = 0, \quad \text{com o parâmetro } \bar{u}_1 = v = 10 \text{ m/s} $$
\\\\
A matriz A mede como os estados influenciam a taxa de mudança uns dos outros.
$$ A = \begin{pmatrix} \frac{\partial \dot{x_2}}{\partial x_2} & \frac{\partial \dot{x_2}}{\partial x_3} \\ \frac{\partial \dot{x_3}}{\partial x_2} & \frac{\partial \dot{x_3}}{\partial x_3} \end{pmatrix}_{(\bar{x}, \bar{u})} $$
Avaliando cada elemento:
\begin{itemize}
    \item $\mathbf{A_{11} = \frac{\partial \dot{x_2}}{\partial x_2}}$: A equação de $\dot{x_2}$ não depende de $x_2$, logo a derivada é $\mathbf{0}$.
    \item $\mathbf{A_{12} = \frac{\partial \dot{x_2}}{\partial x_3}}$: Mede a influência da orientação na velocidade lateral.
    $$ \frac{\partial}{\partial x_3} \left( u_1 \sin(x_3 + \alpha) \right) = u_1 \cos(x_3 + \alpha) $$
    No ponto de equilíbrio ($u_1=10, x_3=0, \alpha=0$): $A_{12} = 10 \cdot \cos(0 + 0) = \mathbf{10}$.
    \item $\mathbf{A_{21} = \frac{\partial \dot{x_3}}{\partial x_2}}$: A equação de $\dot{x_3}$ não depende de $x_2$, logo a derivada é $\mathbf{0}$.
    \item $\mathbf{A_{22} = \frac{\partial \dot{x_3}}{\partial x_3}}$: A equação de $\dot{x_3}$ não depende de $x_3$, logo a derivada é $\mathbf{0}$.
\end{itemize}
Portanto, a matriz de estados é:
\begin{equation}
    A = \begin{pmatrix} 0 & 10 \\ 0 & 0 \end{pmatrix}
\end{equation}
\\\\
A matriz B mede como a entrada (volante, $u_2$) influencia a taxa de mudança dos estados.
$$ B = \begin{pmatrix} \frac{\partial \dot{x_2}}{\partial u_2} \\ \frac{\partial \dot{x_3}}{\partial u_2} \end{pmatrix}_{(\bar{x}, \bar{u})} $$
Avaliando cada elemento:
\begin{itemize}
    \item $\mathbf{B_1 = \frac{\partial \dot{x_2}}{\partial u_2}}$: Mede a influência do volante na velocidade lateral. Usando a regra da cadeia:
    $$ \frac{\partial \dot{x_2}}{\partial u_2} = \frac{\partial}{\partial u_2} \left( u_1 \sin(x_3 + \alpha) \right) = u_1 \cos(x_3 + \alpha) \cdot \frac{\partial \alpha}{\partial u_2} $$
    No equilíbrio, o primeiro termo é $10 \cos(0)=10$. A derivada $\frac{\partial \alpha}{\partial u_2}$ no equilíbrio é $\frac{a}{b} = \frac{1.5}{3.0} = 0.5$.
    Logo, $B_1 = 10 \cdot 0.5 = \mathbf{5}$.
    
    \item $\mathbf{B_2 = \frac{\partial \dot{x_3}}{\partial u_2}}$: Mede a influência do volante na velocidade de rotação. Usando a equação de $\dot{x_3}$ da sua resolução e aplicando a regra da cadeia:
    $$ \frac{\partial \dot{x_3}}{\partial u_2} = \frac{\partial}{\partial u_2} \left( \frac{u_1 \sin(\alpha)}{a} \right) = \frac{u_1 \cos(\alpha)}{a} \cdot \frac{\partial \alpha}{\partial u_2} $$
    No ponto de equilíbrio ($u_1=10, a=1.5, \alpha=0$) e com $\frac{\partial \alpha}{\partial u_2} = 0.5$:
    $$ B_2 = \frac{10 \cdot \cos(0)}{1.5} \cdot 0.5 = \frac{10 \cdot 1}{1.5} \cdot 0.5 = \frac{10}{3} \approx \mathbf{3.33} $$
\end{itemize}
Portanto, a matriz de entrada é:
\begin{equation}
    B = \begin{pmatrix} 5 \\ 10/3 \end{pmatrix}
\end{equation}
\\\\
As matrizes C e D definem como os estados são observados. Definiremos a saída de interesse como sendo apenas a posição lateral, $y = x_2$.

\begin{itemize}
    \item \textbf{Matriz C:} A saída de interesse é o estado $y = x_2$. Para extrair o primeiro estado ($x_2$) do vetor de estados $\tilde{x} = [x_2, x_3]^T$ através da operação $y = C \cdot \tilde{x}$, a matriz C deve ser um vetor linha.
    \begin{equation}
        C = \begin{pmatrix} 1 & 0 \end{pmatrix}
    \end{equation}

    \item \textbf{Matriz D:} Representa a influência direta da entrada na saída. Virar o volante ($u_2$) não muda a posição lateral ($y=x_2$) instantaneamente, apenas a sua taxa de mudança. Portanto, não há alimentação direta e, como o sistema agora é SISO, D é um escalar nulo.
    \begin{equation}
        D = 0
    \end{equation}
\end{itemize}

\subsection*{Conclusão da Tarefa}
O sistema linearizado (A, B, C, D) que aproxima o comportamento do veículo em torno do ponto de equilíbrio de movimento retilíneo, configurado como um sistema de entrada e saída única, é definido pelas seguintes matrizes:
$$ A = \begin{pmatrix} 0 & 10 \\ 0 & 0 \end{pmatrix}, \quad B = \begin{pmatrix} 5 \\ 10/3 \end{pmatrix}, \quad C = \begin{pmatrix} 1 & 0 \end{pmatrix}, \quad D = 0 $$

\subsection*{\sc Questão 1.4}

\subsubsection*{\sc Solução}

Nesta seção, o objetivo é simular e comparar as respostas do sistema em suas representações linear e não linear. Para que a comparação seja significativa, algumas especificações foram definidas com base no sistema linear e também serão adotadas na simulação do sistema não linear.

As especificações consideradas são:
\begin{itemize}
    \item velocidade constante \( v = 10 \, \text{m/s} \);
    \item saída do sistema dada por \( y(t) = x_2(t) \);
    \item entrada única \( u_2(t) = A \), onde \( A \) representa a amplitude de um degrau aplicado igualmente a ambos os sistemas.
\end{itemize}

A amplitude \( A \) corresponde ao ângulo, em radianos, da direção da roda dianteira. Por esse motivo, valores pequenos de \( u_2 \) garantem maior proximidade entre os comportamentos do modelo linear e do não linear, permitindo uma comparação mais coerente.
\\\\
Para simular a resposta de um sistema dinâmico, seja ele linear ou não linear, precisamos resolver suas equações diferenciais. No caso de sistemas não lineares, a abordagem numérica é geralmente a mais viável, enquanto para sistemas lineares, podemos usar a saída a partir do espaço de estados linearizado.
\\\\
Para o sistema não linear, vamos focar na variável de estado $x_2(t)$. A equação diferencial dessa variável de estado, obtida na Questão 1.1, será resolvida utilizando a função \texttt{solve\_ivp()} da biblioteca SciPy em Python para obter os resultados no intervalo de tempo de 0 a 5 segundos. Este método é robusto para resolver Equações Diferenciais Ordinárias (EDOs) e é especialmente útil para sistemas não lineares onde soluções analíticas são complexas.
\\\\
A simulação do sistema linear é baseada no espaço de estados obtido após a linearização na Questão 1.3. As equações de estado são dadas por:

$$
\dot{\textbf{x}} = A\textbf{x}+B\textbf{u}
$$

$$
\textbf{y} = C\textbf{x} + D\textbf{u}
$$

Substituindo pelas matrizes calculadas:

$$
\dot{\textbf{x}} = \begin{bmatrix} 0 & 10 \\ 0 & 0 \end{bmatrix} \cdot \textbf{x} + \begin{bmatrix} 5 \\ 10/3 \end{bmatrix} \cdot \textbf{u}
$$

$$
\textbf{y} = \begin{bmatrix} 1 & 0 \end{bmatrix} \cdot \textbf{x} + 0 \cdot \textbf{u}
$$

Para resolver $y(t)$, podemos aplicar a Transformada de Laplace, admitindo condições iniciais nulas, conforme o Capítulo 3, Seção 6 do livro "Engenharia de Sistemas de Controle" de Nise.
\\\\
Aplicando a Transformada de Laplace às equações de estado, obtemos:

$$
s\textbf{X}(s) = \textbf{AX}(s) + \textbf{BU}(s)
$$

$$
\textbf{Y}(s) = \textbf{CX}(s) + \textbf{DU}(s)
$$

Resolvendo para $\textbf{X}(s)$:

$$
(s\textbf{I} - \textbf{A})\textbf{X}(s) = \textbf{BU}(s)
$$

$$
\textbf{X}(s) = (s\textbf{I} - \textbf{A})^{-1}\textbf{BU}(s)
$$

Agora, substituímos $\textbf{X}(s)$ na equação de saída para encontrar $\textbf{Y}(s)$:

$$
\textbf{Y}(s) = \textbf{C}(s\textbf{I} - \textbf{A})^{-1}\textbf{BU}(s) + \textbf{DU}(s)
$$

Vamos calcular $(s\textbf{I} - \textbf{A})$:

$$
(s\textbf{I} - \textbf{A}) = s\begin{bmatrix} 1 & 0 \\ 0 & 1 \end{bmatrix} - \begin{bmatrix} 0 & 10 \\ 0 & 0 \end{bmatrix} = \begin{bmatrix} s & -10 \\ 0 & s \end{bmatrix}
$$

Para calcular a inversa de $(s\textbf{I} - \textbf{A})$, utilizamos a expressão:

$$
(s\textbf{I} - \textbf{A})^{-1} = \frac{\text{adj}(s\textbf{I} - \textbf{A})}{\text{det}(s\textbf{I} - \textbf{A})}
$$

O determinante de $(s\textbf{I} - \textbf{A})$ é $s \cdot s - (-10) \cdot 0 = s^2$. A matriz adjunta é obtida trocando os elementos da diagonal principal e multiplicando os elementos da diagonal secundária por -1:

$$
\text{adj}(s\textbf{I} - \textbf{A}) = \begin{bmatrix} s & 10 \\ 0 & s \end{bmatrix}
$$

Portanto, a inversa é:

$$
(s\textbf{I} - \textbf{A})^{-1} = \frac{1}{s^2}\begin{bmatrix} s & 10 \\ 0 & s \end{bmatrix} = \begin{bmatrix} \dfrac{1}{s} & \dfrac{10}{s^2} \\ 0 & \dfrac{1}{s} \end{bmatrix}
$$

Finalmente, substituímos na expressão para $\textbf{Y}(s)$. Considerando $\textbf{u}$ como uma entrada degrau com amplitude A, ou seja, $U(s) = A/s$, e como $D=0$:

$$
\textbf{Y}(s) = \begin{bmatrix} 1 & 0 \end{bmatrix} \begin{bmatrix} \dfrac{1}{s} & \dfrac{10}{s^2} \\ 0 & \dfrac{1}{s} \end{bmatrix} \begin{bmatrix} 5 \\ 10/3 \end{bmatrix} U(s)
$$

Primeiro, multiplicamos as matrizes:

$$
\begin{bmatrix} 1 & 0 \end{bmatrix} \begin{bmatrix} \dfrac{1}{s} & \dfrac{10}{s^2} \\ 0 & \dfrac{1}{s} \end{bmatrix} = \begin{bmatrix} \dfrac{1}{s} & \dfrac{10}{s^2} \end{bmatrix}
$$

Obtemos o resultado da operação:

$$
\textbf{Y}(s) = \begin{bmatrix} \dfrac{1}{s} & \dfrac{10}{s^2} \end{bmatrix} \begin{bmatrix} 5 \\ 10/3 \end{bmatrix} U(s)
$$

$$
\textbf{Y}(s) = \left( \dfrac{1}{s} \cdot 5 + \dfrac{10}{s^2} \cdot \dfrac{10}{3} \right) U(s)
$$

$$
\textbf{Y}(s) = \left( \dfrac{5}{s} + \dfrac{100}{3s^2} \right) U(s)
$$

Assumindo que $\textbf{u}$ é uma entrada de degrau, $U(s) = \frac{A}{s}$:

$$
\textbf{Y}(s) = \left( \dfrac{5}{s} + \dfrac{100}{3s^2} \right) \frac{A}{s}
$$

$$
\textbf{Y}(s) = \dfrac{5\cdot A}{s^2} + \dfrac{100 \cdot A}{3s^3}
$$

Para encontrar $y(t)$, aplicamos a Transformada Inversa de Laplace:

$$
y(t) = \mathcal{L}^{-1} \left\{ \dfrac{5\cdot A}{s^2} + \dfrac{100 \cdot A}{3s^3} \right\}
$$

Sabemos que $\mathcal{L}^{-1} \left\{ \dfrac{1}{s^2} \right\} = t$ e $\mathcal{L}^{-1} \left\{ \dfrac{1}{s^3} \right\} = \dfrac{t^2}{2!}$.

Portanto,

$$
y(t) = A \cdot 5t + A \cdot\dfrac{100}{3} \cdot \dfrac{t^2}{2}
$$

$$
y(t) = A \cdot 5t + A \cdot \dfrac{50}{3}t^2
$$

Este é o valor de $y(t)$ para o sistema linearizado sob uma entrada de degrau, assumindo condições iniciais nulas.
\\\\
Considerando o mesmo intervalo de tempo de 0 a 5 segundos para as duas representações, plotamos os comportamentos das respostas a seguir.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{imagens/simulacao-linear-naolinear.png}
    \caption{$x_2$ - Simulação do sistema Não Linear e Linear.}
    \label{fig:simulacao-linear-naolinear}
\end{figure}

Ao analisar o gráfico, é evidente que, para o intervalo de tempo inicial (aproximadamente até 2.5 a 3 segundos), a resposta do sistema linearizado se aproxima significativamente da resposta do sistema não linear. Essa proximidade inicial é esperada, visto que a linearização de um sistema é uma aproximação válida em torno do ponto de equilíbrio.

\begin{lstlisting}[caption = {Código da Questão 1.4}]
import numpy as np
from scipy.integrate import solve_ivp
import matplotlib.pyplot as plt

# Parametros do sistema
a = 1.5
b = 3.0
u1_const = 10.0 

# Amplitude do degrau para o volante (u2)
A = 0.05 

# Intervalo de tempo
t_span = [0, 5]
t_eval = np.linspace(t_span[0], t_span[1], 500)

# Simulacao do Sistema NAO LINEAR
def nonlinear_model(t, x, u1, u2):
    x2, x3 = x
    alpha = np.arctan((a * np.tan(u2)) / b)
    dx2_dt = u1 * np.sin(alpha + x3)
    dx3_dt = (u1 * np.sin(alpha)) / a
    return [dx2_dt, dx3_dt]

x0_nonlinear = [0, 0]
sol_nonlinear = solve_ivp(
    lambda t, x: nonlinear_model(t, x, u1_const, A),
    t_span,
    x0_nonlinear,
    t_eval=t_eval
)
x2_nonlinear = sol_nonlinear.y[0]
# x3_nonlinear = sol_nonlinear.y[1]

# Simulacao do Sistema Linearizado
# Expressoes obtidas a partir do estado de espacos formado apos linearizacao
def funcao_x2_modelo_linear(t, A):
    return (5*t + t**2 * 50/3)*A

# def funcao_x3_modelo_linear(t, A):
#     return (10 * A * t) / 3

plt.style.use('seaborn-v0_8-whitegrid')
plt.figure(figsize=(7, 5))

# Grafico de X2 (Posicao Lateral)
plt.plot(t_eval, x2_nonlinear, 'b-', label='Nao Linear', linewidth=2.5)
plt.plot(t_eval, funcao_x2_modelo_linear(t_eval, A), 'r--', label='Linearizado', linewidth=2)
plt.title(f'Comparacao das Respostas para um Degrau de Amplitude A = {A} rad', fontsize=14)
plt.ylabel('Posicao Lateral $x_2(t)$ (m)', fontsize=12)
plt.xlabel('Tempo $t$ (s)', fontsize=12)
plt.legend()
plt.grid(True)

plt.tight_layout()
plt.show()
\end{lstlisting}

\newpage

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section*{\sc Tarefa 2: Análise Dinâmica}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection*{\sc Questão 2.1}

\subsubsection*{\sc Solução}

Para iniciarmos a análise do sistema, é preciso converter o modelo de espaço de estados linearizado, obtido na Tarefa 1, em uma função de transferência $T(s)$ que relaciona a entrada de controle (ângulo do volante, $u_2$) com a saída de interesse (posição lateral, $x_2$).
\\\\
Para chegar à expressão da função transferência, começamos utilizando a propriedade da derivada no domínio de Laplace, $\mathcal{L}\{\dot{f}(t)\} = sF(s) - f(0)$, na Equação (1):
\begin{align*}
    \mathcal{L}\{\dot{\mathbf{x}}(t)\} &= \mathcal{L}\{A\mathbf{x}(t) + B\mathbf{u}(t)\} \\
    s\mathbf{X}(s) - \mathbf{x}(0) &= A\mathbf{X}(s) + B\mathbf{U}(s)
\end{align*}

Assumindo que o estado inicial do sistema é nulo, ou seja, $\mathbf{x}(0) = \mathbf{0}$. Isso simplifica a equação anterior para:
\begin{equation*}
    s\mathbf{X}(s) = A\mathbf{X}(s) + B\mathbf{U}(s)
\end{equation*}

Realizando manipulações algébricas na equação, chegamos a:
$\mathbf{X}(s)$:
\begin{equation*}
    \mathbf{X}(s) = (sI - A)^{-1}B\mathbf{U}(s)
\end{equation*}

Para a equação de saída, aplicamos também a transformada:
\begin{align*}
    \mathcal{L}\{\mathbf{y}(t)\} &= \mathcal{L}\{C\mathbf{x}(t) + D\mathbf{u}(t)\} \\
    \mathbf{Y}(s) &= C\mathbf{X}(s) + D\mathbf{U}(s)
\end{align*}

Substituindo $\mathbf{X}(s)$ pela equação encontrada e realizando manipulações chegamos em:

\begin{equation*}
    \mathbf{Y}(s) = \left[ C(sI - A)^{-1}B + D \right] \mathbf{U}(s)
\end{equation*}
Ao reorganizar a equação para a forma $\frac{\mathbf{Y}(s)}{\mathbf{U}(s)}$, obtemos a fórmula final para a função de transferência:
\begin{equation*}
    \boxed{G(s) = C(sI - A)^{-1}B + D}
\end{equation*}

Onde $I$ é a matriz identidade.\\


As matrizes do nosso sistema linearizado, conforme derivado e ajustado na Tarefa 1, são:
$$ A = \begin{pmatrix} 0 & 10 \\ 0 & 0 \end{pmatrix}, \quad B = \begin{pmatrix} 5 \\ 10/3 \end{pmatrix}, \quad C = \begin{pmatrix} 1 & 0 \end{pmatrix}, \quad D = 0 $$

Prosseguindo, calculamos a matriz característica do sistema.
\begin{align*}
    sI - A &= s\begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix} - \begin{pmatrix} 0 & 10 \\ 0 & 0 \end{pmatrix} \\
           &= \begin{pmatrix} s & 0 \\ 0 & s \end{pmatrix} - \begin{pmatrix} 0 & 10 \\ 0 & 0 \end{pmatrix} \\
           &= \begin{pmatrix} s & -10 \\ 0 & s \end{pmatrix}
\end{align*}

Agora, calculamos a inversa da matriz encontrada. Para uma matriz 2x2, $\begin{pmatrix} a & b \\ c & d \end{pmatrix}$, a inversa é $\frac{1}{ad-bc}\begin{pmatrix} d & -b \\ -c & a \end{pmatrix}$.
O determinante de $(sI - A)$ é:
$$ \det(sI - A) = (s)(s) - (-10)(0) = s^2 $$
Portanto, a matriz inversa é:
\begin{equation}
    (sI - A)^{-1} = \frac{1}{s^2} \begin{pmatrix} s & 10 \\ 0 & s \end{pmatrix}
\end{equation}

Em seguida, primeiramente multiplicamos a inversa pela matriz C:
\begin{align*}
    C(sI - A)^{-1} &= \begin{pmatrix} 1 & 0 \end{pmatrix} \cdot \frac{1}{s^2} \begin{pmatrix} s & 10 \\ 0 & s \end{pmatrix} \\
                   &= \frac{1}{s^2} \begin{pmatrix} (1 \cdot s + 0 \cdot 0) & (1 \cdot 10 + 0 \cdot s) \end{pmatrix} \\
                   &= \frac{1}{s^2} \begin{pmatrix} s & 10 \end{pmatrix}
\end{align*}
depois multiplicamos o resultado pela matriz B:
\begin{align*}
    [C(sI - A)^{-1}]B &= \frac{1}{s^2} \begin{pmatrix} s & 10 \end{pmatrix} \cdot \begin{pmatrix} 5 \\ 10/3 \end{pmatrix} \\
                     &= \frac{1}{s^2} \left( (s \cdot 5) + (10 \cdot \frac{10}{3}) \right) \\
                     &= \frac{5s + \frac{100}{3}}{s^2}
\end{align*}

O passo final é somar o termo D, que no nosso caso é 0.
$$ G(s) = \frac{5s + \frac{100}{3}}{s^2} + 0 $$

Após realizarmos estes cálculos, chegamos à seguinte função de transferência:
\begin{equation}
    G(s) = \frac{5s + \frac{100}{3}}{s^2} \approx \frac{5s + 33.33}{s^2}
\end{equation}
\newpage

\begin{figure}[h!]
     \centering
     \includegraphics[width=0.7\textwidth]{imagens/Plot_t2q1.png}
\end{figure}

\begin{lstlisting}[caption = {Código da Questão 2.1}, label={code:t2q1}]

import control as ct
import matplotlib.pyplot as plt
import numpy as np


# A funcao de transferencia calculada foi T(s) = (5s + 33.33) / s^2

#coeficientes
num = [5, 100/3]
den = [1, 0, 0]

T = ct.tf(num, den)

#print(T)

#escolhido por padrao da .step_response degrau de amplitude 1.
t, y = ct.step_response(T)


plt.figure(figsize=(10, 6))
plt.plot(t, y, linewidth=2, label='Resposta ao Degrau')

plt.title('Resposta ao Degrau do Sistema Linearizado', fontsize=16)
plt.xlabel('Tempo (s)', fontsize=12)
plt.ylabel('Posicao Lateral ($x_2$)', fontsize=12)
plt.grid(True)
plt.legend()
plt.show()

\end{lstlisting}
\newpage
\subsection*{\sc Questão 2.2}

\subsubsection*{\sc Solução}
Para iniciarmos essa questão, vamos definir cada uma das métricas:
\begin{itemize}
    \item Valor de Estado Estacionário: É o valor final que a saída do sistema atinge e se mantém após todas as perturbações transitórias terem se dissipado, ou seja, quando o tempo $t$ tende ao infinito ($t \to \infty$). É o comportamento de longo prazo do sistema.
    \item Porcentagem de Sobressinal: É a quantidade máxima pela qual a resposta do sistema excede seu valor de estado estacionário, expressa como uma porcentagem do valor de estado estacionário. 
    \item Tempo de Subida: É o tempo que a resposta do sistema leva para ir de 10\% a 90\% do valor de estado estacionário
    \item Tempo de Estabilização: É o tempo que a resposta do sistema leva para atingir e permanecer dentro de uma banda de erro especificada do valor de estado estacionário.
\end{itemize}

Analisando cada um desses valores para o nosso gráfico, temos:
\begin{itemize}
    \item Valor de Estado Estacionário: A resposta cresce indefinidamente com o tempo, de modo que não é possível determinar um valor de estado estacionário
    \item Porcentagem de Sobressinal: Da mesma forma, também não é possível determinar uma porcentagem de sobressinal, o sistema é instável.
    \item Tempo de Subida: Não é possível determinar um tempo de subida, a resposta sobe/cresce indefinidamente.
    \item Tempo de Estabilização: A resposta não se estabiliza, não é possível determinar um tempo de estabilização. (Ou também podemos considerar o tempo de estabilização infinito).
\end{itemize}

\newpage
\subsection*{\sc Questão 2.3}

\subsubsection*{\sc Solução}
O mapa polo-zero é uma representação gráfica dos polos(valores de s para os quais a função de transferência se torna infinita) e zeros(valores de s para os quais a função de transferência se torna 0). Os polos são representados por $\times$ no mapa e os zeros por $\circ$. \\

\begin{figure}[h!]
     \centering
     \includegraphics[width=0.7\textwidth]{imagens/Plot_t2q3.png}
\end{figure}

Através da expressão $G(s) = \frac{5s + \frac{100}{3}}{s^2} \approx \frac{5s + 33.33}{s^2}$ e do plot. Podemos verificar que há um zero em $s \approx -6.67$ e dois polos (multiplicidade 2) em $s = 0$.
\\\\
Com esse resultado podemos inferir que o sistema é instável, pois polos múltiplos sobre o eixo imaginário ($Re\{s\} = 0$) indicam instabilidade.
\\\\
Além disso, apesar de não influenciarem na estabilidade, os zeros influenciam "acelerando" a resposta do sistema. Nesse caso, o zero altera a inclinação da rampa de crescimento da nossa resposta.


\begin{lstlisting}[caption = {Código da Questão 2.3}, label={code:t2q3}]
import control as ct
import numpy as np
import matplotlib.pyplot as plt

#coeficientes da transfer function
num = [5, 33.33]
den = [1, 0, 0]

sys = ct.TransferFunction(num, den)
print(sys)

plt.figure(figsize=(8,6))

ct.pzmap(sys, plot=True, title='Mapa Polo-Zero')
plt.xlabel('Eixo Real ($\sigma$)')
plt.ylabel('Eixo Imaginario ($j\omega$)')
plt.show()
\end{lstlisting}

\subsection*{\sc Questão 2.4}

\subsubsection*{\sc Solução}

No contexto do sistema mecânico (o veículo), as variáveis são definidas da seguinte forma:
\\\\
Variáveis de Estado:
\begin{itemize}
    \item $x_1(t)$ (posição longitudinal)
    \item $x_2(t)$ (posição lateral)
    \item $x_3(t) = \theta(t)$ (orientação)
\end{itemize}
Vetor de Entradas ($u(t)$):
\begin{itemize}
    \item $u(t) = [v(t), \delta(t)]$, onde $v(t)$ é a velocidade no centro de massa e $\delta(t)$ é o ângulo de direção.
\end{itemize}
Vetor de Saídas ($y(t)$:
\begin{itemize}
    \item $y(t) = [x_1(t), x_2(t)]$.
\end{itemize}

No entanto, para a análise de controlabilidade e observabilidade do sistema linearizado, as fontes especificam uma configuração reduzida:
\begin{itemize}
    \item A variável manipulada (entrada) é o ângulo de esterçamento ($\delta$).
    \item A variável controlada (saída) é a posição lateral ($x_2$).
    \item As variáveis de estado consideradas para este modelo linearizado e reduzido são $x_2$ e $x_3$.
\end{itemize}

Para verificar a controlabilidade de um sistema, você utiliza a Matriz de Controlabilidade ($C_M$). Para um sistema de $n$ variáveis de estado descrito por $\dot{x} = Ax + Bu$, a matriz de controlabilidade é dada por $C_M = [B \quad AB \quad A^2B \quad \dots \quad A^{n-1}B]$. O sistema é completamente controlável se, e somente se, o posto (rank) da matriz $C_M$ for igual a $n$.
\\\\
O modelo linearizado e reduzido do seu sistema, focado em $x_2$ e $x_3$, possui as seguintes matrizes A e B: $A = \begin{bmatrix} 0 & 10 \\ 0 & 0 \end{bmatrix}$ $B = \begin{bmatrix} 5 \\ 10/3 \end{bmatrix}$.
\\\\
O número de variáveis de estado ($n$) é 2 e a matriz de controlabilidade $C_M$ é $[B \quad AB]$.
\\\\
Resolvendo AB:
\[
 AB = \begin{bmatrix} 0 & 10 \\ 0 & 0 \end{bmatrix} \begin{bmatrix} 5 \\ 10/3 \end{bmatrix} = \begin{bmatrix} 10 \cdot (10/3) \\ 0 \end{bmatrix} = \begin{bmatrix} 100/3 \\ 0 \end{bmatrix}
\]
 \\\\
 Assim:
 \[
    C_M = \begin{bmatrix} 5 & 100/3 \\ 10/3 & 0 \end{bmatrix}
 \]
 Para encontrar o posto desta matriz 2x2, calculamos seu determinante: $det(C_M) = (5 \cdot 0) - (100/3 \cdot 10/3) = 0 - 1000/9 = -1000/9$. Como o determinante é diferente de zero, o posto da matriz $C_M$ é 2.
 \\\\
Uma vez que o posto é igual ao número de variáveis de estado, o sistema é completamente controlável.
\\\\
A observabilidade de um sistema refere-se à capacidade de saber o que está acontecendo dentro do sistema apenas olhando para suas saídas.
\\\\
Para verificar a observabilidade, você utiliza a Matriz de Observabilidade ($O$). Para um sistema descrito por $\dot{x} = Ax + Bu$ e $y = Cx$, a matriz de observabilidade é dada por $O = \begin{bmatrix} C \ CA \ CA^2 \ \dots \ CA^{n-1} \end{bmatrix}$. O sistema é completamente observável se, e somente se, o posto (rank) da matriz $O$ for igual a $n$.
\\\\
As matrizes A e C para o modelo linearizado e reduzido são: $A = \begin{bmatrix} 0 & 10 \\ 0 & 0 \end{bmatrix}$ $C = \begin{bmatrix} 1 & 0 \end{bmatrix}$ (considerando $x_2$ como a saída controlada).
\\\\
A matriz de observabilidade $O$ é $\begin{bmatrix} C \\ CA \end{bmatrix}$.
\\\\
Resolvendo CA:
\[
    CA = \begin{bmatrix} 1 & 0 \end{bmatrix} \begin{bmatrix} 0 & 10 \\ 0 & 0 \end{bmatrix} = \begin{bmatrix} 0 & 10 \end{bmatrix}
\]
Assim:
\[
    O = \begin{bmatrix} 1 & 0 \\ 0 & 10 \end{bmatrix}
\]
Calculamos seu determinante: $det(O) = (1 \cdot 10) - (0 \cdot 0) = 10 - 0 = 10$. Como o determinante é diferente de zero, o posto da matriz $O$ é 2. 
\\\\
Uma vez que o posto é igual ao número de variáveis de estado, o modelo linearizado é completamente observável.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section*{\sc Tarefa 3: Controle do Sistema Dinâmico}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection*{\sc Questão 3.1}

\subsubsection*{\sc Solução}

Para a elaboração do diagrama de blocos do nosso sistema de controle, é fundamental compreender a função e a interconexão de cada componente. Abaixo, detalhamos os elementos essenciais e sua relevância para o sistema.
\\\\
O sistema a ser controlado, ou planta, é o próprio veículo. Suas dinâmicas são representadas pelo modelo linearizado obtido anteriormente. Este modelo descreve o comportamento do veículo em termos de desvios, focando especificamente na posição lateral ($x_2(t)$) e na orientação ($x_3(t)$). A entrada ($u$) que manipula essas variáveis é o desvio no ângulo de direção ($\delta$) da roda dianteira.
\\\\
O transmissor ou sensor é o componente responsável por medir a variável de saída do sistema, que será utilizada para realimentação no controlador. No nosso caso, a saída controlada é a posição lateral do veículo, $x_2(t)$. Portanto, o sensor medirá essa posição lateral e enviará essa informação ao controlador.
\\\\
O controlador desempenha um papel central no sistema. Ele recebe dois sinais: a saída desejada (referência) e a saída medida pelo sensor. Com base na diferença entre esses dois sinais (o erro), o controlador gera um sinal de controle que será enviado ao atuador. Um objetivo primordial do nosso projeto é que o controlador não apenas estabilize o sistema, mas também o mantenha dentro de uma faixa de valores com um erro de estado estacionário previsto, conforme as especificações.
\\\\
Por fim, o atuador é o elemento que converte o sinal de controle recebido do controlador em uma ação física no sistema. A entrada manipulada para o controle do veículo é o ângulo de direção ($\delta(t)$) da roda dianteira. Sendo assim, o atuador corresponde ao mecanismo de direção do veículo, que fisicamente ajusta o ângulo das rodas dianteiras em resposta ao sinal de controle.
\\\\
Após contextualizar os elementos importantes para o diagrama, temos: 
\begin{figure}[H] % [h!] tenta colocar a figura "aqui" (here)
    \centering % Centraliza a imagem
    \includegraphics[width=0.8\textwidth]{imagens/Diagrama de Blocos.drawio (3).pdf} % Inclui o PDF como imagem
    \caption{Diagrama de Blocos do Sistema de Controle.} % Legenda para a figura
    \label{fig:diagrama_blocos} % Rótulo para referências cruzadas
\end{figure}

\subsection*{\sc Questão 3.2}

\subsubsection*{\sc Solução}

O erro de estado estacionário ($e(\infty)$) é a diferença entre a entrada desejada (referência) e a saída real do sistema depois que o tempo de resposta transitória se esgotou, ou seja, quando $t \to \infty$. Ele representa a precisão do sistema em malha fechada em rastrear uma entrada específica.
\\\\
Considerando o erro do ponto de vista do diagrama de blocos.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{imagens/sistema-malha-fechada.png}
    \caption{Sistema de malha fechada com feedback unitário.}
    \label{fig:simulacao-linear-naolinear}
\end{figure}
\begin{itemize}
    \item $R(s)$ é a entrada
    \item $C(s) = E(s)G(s)$ é a saída
    \item $E(s) = R(s) - C(s)$ é o erro 
\end{itemize}
Resolvendo para $E(s)$:
\[
    E(s) = \frac{R(s)}{1 + G(s)}
\]
Aplicamos o teorema do valor final, para obter:
\[
    e(\infty) = \lim_{s \to 0} \frac{sR(s)}{1 + G(s)}
\]
Para uma entrada degrau:
\[
    e(\infty) = \lim_{s \to 0} \frac{s\frac{1}{s}}{1 + G(s)}
\]
\[
    e(\infty) = \lim_{s \to 0} \frac{1}{1 + G(s)}
\]

Tendo sido apresentado o requisito de erro de estado estacionário, agora, vamos definir o controlador a ser utilizado. Com base nas especificações que exigem um projeto para obter um erro de estado estacionário de resposta ao degrau de 10\% sem afetar significativamente a resposta transitória, optaremos por um compensador do tipo Lag Compensator (Compensador de Atraso).
\\\\
Função de transferência do controlador:
\[
    G_{c}(s) = \frac{s+z_c}{s+p_c}
\]
Projetando o sistema com controlador:
\begin{figure}[H] % [h!] tenta colocar a figura "aqui" (here)
    \centering % Centraliza a imagem
    \includegraphics[width=0.8\textwidth]{imagens/Diagrama de Blocos-Página-3.drawio (1).pdf} % Inclui o PDF como imagem
    \caption{Sistema com Controlador.} % Legenda para a figura
    \label{fig:diagrama_blocos_controlador} % Rótulo para referências cruzadas
\end{figure}
Em que T(s) é a função de transferência do nosso sistema ($G(s)$) em malha fechada com realimentação unitária. Utilizamos T(s) (ou seja, uma abordagem que nos permita influenciar o erro de estado estacionário para um valor diferente de zero), porque, de outro modo, a função de transferência de malha aberta G(s), obtida na questão 2.1, não permitiria um erro de estado estacionário diferente de zero. Isso ocorre porque G(s) possui duas integrações puras no caminho direto da malha, caracterizando-a como um sistema do Tipo 2.
\\\\
Explicado o motivo de usarmos $T(s)$ como a função de transferência da planta no cálculo do erro de estado estacionário, temos a expressão para $T(s)$ como:
\[
    T(s) = \frac{5s+33.33}{s^2} \cdot \dfrac{1}{1 + \dfrac{5s + 33.33}{s^2}}
\]
Simplificando, obtemos:
\[
    T(s) = \frac{5s + 33.33}{s^2 + 5s+33.33}
\]

Para o erro de estado estacionário do sistema em malha fechada para uma entrada degrau, com a inclusão do controlador $G_c(s)$, a fórmula é:
\[
    e(\infty) = \lim_{s \to 0} \frac{1}{1 + G_{c}(s)T(s)}
\]
Segundo o enunciado, queremos projetar o controlador para obter 10\% de erro de estado estacionário. Assim, temos:
\[
    0.1 = \frac{1}{1 + \lim_{s \to 0}G_{c}(s)T(s)}
\]
Reorganizando a equação, chegamos a:
\[
    1 + \lim_{s \to 0}G_{c}(s)T(s) = 10
\]
\[
    G_{c}(0)T(0) = 9
\]
Considerando a forma do compensador de atraso $G_c(s) =\frac{s + z_c}{s + p_c}$ e avaliando $T(s)$ para $s \to 0$:
\[
T(0) = \frac{5 \cdot 0 + 33.33}{0^2 + 5 \cdot 0 + 33.33} = \frac{33.33}{33.33} = 1
\]
Agora, substituindo $G_c(0)$ e $T(0)$ na equação $G_{c}(0)T(0) = 9$:
\[
\frac{0 + z_c}{0 + p_c} \cdot 1 = 9
\]
\[
\frac{z_c}{p_c} = 9
\]
O valor $z_c/p_c = 9$ representa o fator de melhoria do erro de estado estacionário que o compensador de atraso deve proporcionar. Pelo padrão desse compensador, sabemos que devemos escolher o zero ($z_c$) e o polo ($p_c$) próximos da origem e no semiplano esquerdo, com o polo mais próximo da origem que o zero ($|p_c| < |z_c|$).
\\\\
Para não afetar a resposta transitória de forma apreciável, esses polo e zero devem estar muito próximos da origem e bem afastados dos polos dominantes do sistema. Para isso, vamos assumir o zero do compensador, $z_c$, como 50 vezes menor em magnitude que a parte real do polo dominante do sistema $T(s)$.
\\\\
Os polos do sistema são obtidos da equação $s^2 + 5s + 33.33 = 0$, que são:
\[
    p = -2.5 \pm 5.2038j
\]
Assim, o zero do compensador é calculado como:
\[
    z_c = \left| \frac{\text{real}(\text{polo dominante do sistema)}}{50} \right| = \left| \frac{-2.5}{50} \right| = |-0.05| = 0.05
\]
Em seguida, para determinar o polo do compensador ($p_c$), utilizamos o fator de melhoria $\frac{z_c}{p_c} = 9$, que foi derivado do requisito de 10\% de erro de estado estacionário:
\[
p_c = \frac{z_c}{9} = \frac{0.05}{9} \approx 0.0055
\]
Dessa forma, a função de transferência do compensador de atraso projetado ($G_c(s)$) é:
\[
G_{c}(s) = \frac{s + 0.05}{s + 0.0055}
\]

\subsection*{\sc Questão 3.3}

\subsubsection*{\sc Solução}

Para simular e analisar a resposta ao degrau do sistema, consideraremos as configurações não compensada (sem controlador) e compensada (com o controlador de atraso projetado).
\\\\
Conforme mencionado, para a análise e o projeto de compensação, estamos considerando T(s) como a representação do nosso sistema. Assim, o sistema não compensado terá a seguinte configuração:

\begin{figure}[H] % [h!] tenta colocar a figura "aqui" (here)
    \centering % Centraliza a imagem
    \includegraphics[width=0.8\textwidth]{imagens/Diagrama de Blocos-Página-2.drawio.pdf} % Inclui o PDF como imagem
    \caption{Sistema T(s) sem Controlador.} % Legenda para a figura
    \label{fig:diagrama_blocos_controlador} % Rótulo para referências cruzadas
\end{figure}

A função de transferência de malha aberta do sistema não compensado é dada por $T(s)$:
\[
    T(s) = \frac{5s + 33.33}{s^2 + 5s+33.33}
\]
Para uma entrada degrau unitário, $U(s)= \frac{1}{s}$, no sistema não compensado de malha fechada com feedback unitário, a resposta no domínio de Laplace é:
\[
    Y(s) = U(s) \cdot \frac{T(s)}{1 + T(s)} =\frac{1}{s}\cdot\frac{5s + 33.33}{s^2 + 10s+66.66}
\]

A seguir, é apresentada a resposta ao degrau do sistema não compensado: 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{imagens/resposta-degrau-sem-controlador.png}
    \caption{Resposta ao degrau sem controlador.}
    \label{fig:resposta-degrau-sem-controlador}
\end{figure}

Observa-se que a resposta ao degrau do sistema não compensado se estabiliza em 0.5. Isso indica que o erro de estado estacionário é de 50\% ($e_{ss} = 1 - 0.5 = 0.5$) para entrada degrau unitário.
\\\\
Para simular o sistema compensado, aplicando o Lag Compensator $G_c(s) = \frac{s + 0.05}{s + 0.0055}$em cascata com a planta e realimentação unitária, a função de transferência de malha fechada é dada por:
\[
    Y(s) = U(s) \cdot \frac{G_c(s)T(S)}{1 + G_c(s)T(S)}
\]
Substituindo $G_c(s)$ e $T(s)$ na expressão:
\[
    Y(s) = \frac{1}{s} \cdot \frac{5 s^2 + 33.58 s + 1.667}{s^3 + 10.01 s^2 + 66.94 s + 1.852}
\]
A seguir, é apresentada a resposta ao degrau do sistema compensado:
\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{imagens/resposta-degrau-com-controlador.png}
    \caption{Resposta ao degrau com controlador.}
    \label{fig:resposta-degrau-com-controlador}
\end{figure}

Observa-se que a resposta ao degrau do sistema compensado se estabiliza em 0.9. O resultado indica erro estacionário de cerca de 10\% ($e_{ss} = 1 - 0.9=0.1$) para entrada degrau unitário. Este é o comportamento desejado e a meta do projeto do compensador.

\begin{lstlisting}[caption = {Código da Questão 3.3}]
import matplotlib.pyplot as plt
import control as ctr
\end{lstlisting}
\begin{lstlisting}[caption = {Código da Questão 3.3}]
G_s = ctr.tf([5, 33.33], [1, 0, 0])
print(G_s)

t, y = ctr.step_response(G_s)
plt.plot(t, y)
plt.show()
\end{lstlisting}
\begin{lstlisting}[caption = {Código da Questão 3.3}]
T_s = ctr.feedback(G_s, 1)
T_s_feedback = ctr.feedback(T_s, 1)
print(T_s_feedback)

polos_G_s_feedback = ctr.poles(T_s_feedback)  
print(f'\nPolos do feedback de G_s(s): {polos_G_s_feedback}')

t, y = ctr.step_response(T_s_feedback)
plt.plot(t, y)
plt.xlabel('Tempo (s)')
plt.ylabel('Resposta y(t)')
plt.grid()
plt.show()
ctr.step_info(T_s_feedback)
\end{lstlisting}
\begin{lstlisting}[caption = {Código da Questão 3.3}]
G_c = ctr.tf([1, 0.05], [1, 0.005555])

G = T_s * G_c

G_feedback = ctr.feedback(G, 1)

print(G_feedback)

t, y = ctr.step_response(G_feedback)
plt.plot(t, y)
plt.xlabel('Tempo (s)')
plt.ylabel('Resposta y(t)')
plt.grid()
plt.show()
ctr.step_info(G_feedback)
\end{lstlisting}


\subsection*{\sc Questão 3.4}

\subsubsection*{\sc Solução}

Na elaboração do sistema de controle, a escolha por um compensador de atraso (Lag Compensator) foi motivada pela necessidade de atender a uma especificação clara: garantir um erro de estado estacionário de 10\% frente a uma entrada degrau, sem afetar significativamente a resposta transitória. O sistema original, com função de transferência $G(s)$, por ser de tipo 2 (com duas integrações puras em malha aberta), naturalmente apresentava erro de estado estacionário nulo, o que é incompatível com o erro especificado de 10\%. Por isso, optou-se por usar a função de transferência em malha fechada para projetar o controlador, o que permite ajustar o erro final a um valor diferente de zero.
\\\\
O compensador foi projetado de forma que seu zero ($z_c$) ficasse próximo da origem, mas suficientemente afastado dos polos dominantes do sistema para não interferir na dinâmica transitória. O polo ($p_c$), ainda mais próximo da origem, foi escolhido de modo a garantir uma razão $z_c/p_c = 9$, fornecendo exatamente o ganho necessário para atingir o erro de 10\%. A validação foi feita por meio da resposta ao degrau: sem o controlador, a saída final é 1 (erro zero); com o controlador, a resposta se estabiliza em 0.9, resultando em um erro estacionário de 10\%, exatamente como projetado. Esse resultado comprova o sucesso do projeto e a eficácia da estratégia adotada.

\end{document}